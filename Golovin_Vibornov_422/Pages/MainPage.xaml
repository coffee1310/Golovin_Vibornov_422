<Page x:Class="Golovin_Vibornov_422.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Golovin_Vibornov_422.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Все объявления">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#0078D4" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="← Назад" Click="BackButton_Click" />

                <TextBlock Grid.Column="1" Text="Все объявления" 
                          FontSize="20" FontWeight="Bold" Foreground="White"
                          HorizontalAlignment="Center" VerticalAlignment="Center"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBlock Text="🌐" FontSize="16" Foreground="White" 
                              VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="Публичный доступ" FontSize="12" Foreground="White" 
                              VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <Button Content="🔐 Войти" Click="LoginButton_Click"
                            Background="#107C10" Foreground="White"
                            Padding="10,5" BorderThickness="0"
                            FontSize="12" FontWeight="SemiBold"
                            Cursor="Hand"
                            ToolTip="Перейти на страницу авторизации"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="#F5F5F5" BorderBrush="#E0E0E0" 
                BorderThickness="0,0,0,1" Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="txtSearch" Grid.Column="0" 
                        TextChanged="txtSearch_TextChanged"
                        Padding="10,5" FontSize="12"
                        ToolTip="Поиск по заголовку и описанию">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="BorderBrush" Value="#CCCCCC"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="BorderBrush" Value="#0078D4"/>
                                </Trigger>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter Property="BorderBrush" Value="#0078D4"/>
                                    <Setter Property="BorderThickness" Value="2"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <ComboBox x:Name="cmbCity" Grid.Column="1" 
                         SelectionChanged="Filter_SelectionChanged"
                         DisplayMemberPath="city1"
                         Width="120" Margin="10,0,0,0"
                         ToolTip="Фильтр по городу">
                    <ComboBox.Style>
                        <Style TargetType="ComboBox">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="BorderBrush" Value="#CCCCCC"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="FontSize" Value="12"/>
                        </Style>
                    </ComboBox.Style>
                </ComboBox>

                <!-- Фильтр по категории -->
                <ComboBox x:Name="cmbCategory" Grid.Column="2" 
                         SelectionChanged="Filter_SelectionChanged"
                         DisplayMemberPath="name"
                         Width="130" Margin="10,0,0,0"
                         ToolTip="Фильтр по категории">
                    <ComboBox.Style>
                        <Style TargetType="ComboBox">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="BorderBrush" Value="#CCCCCC"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="FontSize" Value="12"/>
                        </Style>
                    </ComboBox.Style>
                </ComboBox>

                <ComboBox x:Name="cmbType" Grid.Column="3" 
                         SelectionChanged="Filter_SelectionChanged"
                         DisplayMemberPath="type1"
                         Width="140" Margin="10,0,0,0"
                         ToolTip="Фильтр по типу объявления">
                    <ComboBox.Style>
                        <Style TargetType="ComboBox">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="BorderBrush" Value="#CCCCCC"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="FontSize" Value="12"/>
                        </Style>
                    </ComboBox.Style>
                </ComboBox>

                <!-- Фильтр по статусу -->
                <ComboBox x:Name="cmbStatus" Grid.Column="4" 
                         SelectionChanged="Filter_SelectionChanged"
                         DisplayMemberPath="status1"
                         Width="120" Margin="10,0,0,0"
                         ToolTip="Фильтр по статусу">
                    <ComboBox.Style>
                        <Style TargetType="ComboBox">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="BorderBrush" Value="#CCCCCC"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="FontSize" Value="12"/>
                        </Style>
                    </ComboBox.Style>
                </ComboBox>

                <!-- Кнопка сброса фильтров -->
                <Button Grid.Column="5" Content="🗑️ Сбросить" 
                        Click="ResetFilters_Click"
                        Margin="10,0,0,0"
                        ToolTip="Сбросить все фильтры"/>

                <Button Grid.Column="6" Content="🔄 Обновить" 
                        Click="RefreshButton_Click"
                        Margin="10,0,0,0"
                        ToolTip="Обновить данные"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="15">
            <ItemsControl x:Name="itemsAds">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1"
                                CornerRadius="8" Margin="5" Padding="0" 
                                Width="280" Height="380">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="180"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0" Background="#F5F5F5" 
                                        CornerRadius="8,8,0,0">
                                    <Image x:Name="imgAd" 
                                           Stretch="UniformToFill" 
                                           ToolTip="Изображение объявления">
                                        <Image.Style>
                                            <Style TargetType="Image">
                                                <Setter Property="Source" Value="/Images/no-image.jpg"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding HasImage}" Value="True">
                                                        <Setter Property="Source" Value="{Binding ImageSource}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Image.Style>
                                    </Image>
                                </Border>

                                <TextBlock Grid.Row="1" Text="{Binding ad_title}" 
                                          FontWeight="Bold" FontSize="14" 
                                          Margin="10,8,10,5" 
                                          TextWrapping="Wrap" 
                                          MaxHeight="40"
                                          TextTrimming="CharacterEllipsis"/>

                                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" 
                                             Margin="10,0,10,5">
                                    <TextBlock Text="{Binding ad_description}" 
                                              FontSize="12" TextWrapping="Wrap"
                                              Foreground="#666666"
                                              MaxHeight="80"/>
                                </ScrollViewer>

                                <Border Grid.Row="3" Background="#F9F9F9" 
                                       Margin="0,5,0,0" Padding="10,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0" Orientation="Vertical">
                                            <TextBlock Text="{Binding City.city1}" 
                                                      FontSize="11" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding AdType.type1}" 
                                                      FontSize="10" Foreground="#666666"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="1" Orientation="Vertical" 
                                                   HorizontalAlignment="Right">
                                            <TextBlock Text="{Binding price, StringFormat={}{0}₽}" 
                                                      FontSize="14" FontWeight="Bold" 
                                                      Foreground="#0078D4"/>
                                            <TextBlock Text="{Binding ad_post_date, StringFormat=dd.MM.yyyy}" 
                                                      FontSize="9" Foreground="#999999"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <Border Grid.Row="4" Background="{Binding StatusColor}" 
                                       CornerRadius="0,0,8,8" Padding="5,2">
                                    <TextBlock Text="{Binding AdStatus.status1}" 
                                              FontSize="10" FontWeight="Bold" 
                                              Foreground="White" 
                                              HorizontalAlignment="Center"/>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StatusBar Grid.Row="3" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <StatusBarItem>
                <TextBlock x:Name="lblStatus" Text="Готово" FontSize="11" Foreground="#333333"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="lblCount" Text="Найдено объявлений: 0" FontSize="11" Foreground="#666666"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="lblFilterInfo" Text="Фильтры не применены" FontSize="11" Foreground="#999999"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Page>